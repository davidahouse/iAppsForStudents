
<div id="intro2">
<p>Add any Mac, iPhone, or iPad software that you wish to participate in the Back to School Sale.</p> 
<p>Due to the nature of the AppStore iPhone and iPad apps will need to be discounted throughout the AppStore during the sale period.</p>
<p>When the sale starts your software will be listed as shown below.</p>
</div>

<hr class="top-10"/>

<h3><%= @company.title %></h3>

<% for a in @applications %>

<div class="span-24">
	<div class="appinfobckgnd-edit">
		<div class="appinfo">
	 
			<div class="span-4">
				<% if a.icon.exists? %>
				<%= image_tag a.icon.url(:small) %>
				<% end %>
			</div>

			<div class="span-12">
				<div class="apptitle">
					<%= a.title %>
				</div>
			</div>
			
			<div class="span-12">
				<%= a.description %>
     		</div>
		

		<!-- <div class="span-10 last"> -->
		<div class="span-4 appinforight">
		
			<div class="burst">
				<div class="burst_content">
					<% if ( ( a.discount != nil ) && ( a.discount > 0.0 ) )%>
						<div class="burst_percent">
							<%= a.discount.to_i %> %
							<div class="percentoff">
						       Off
							</div>
						</div>
					<% end %>
				</div>
			</div>
			
			<div class="app_price">
				<%= a.sale_price %> (<%= a.price_currency%>)
			</div>
				
				<div class="now">
					<% if a.url.include? "http" %>
						<a href="<%= a.url %>">Get it Now!</a>
					<% else %>
						<a href="http://<%= a.url %>">Get it Now!</a>
					<% end %>
				</div>
				
				<div class="disccode">
				<% if a.discount_code != '' %>
				
					<div>Discount Code</div>
					<%= a.discount_code %>
				
				<% else %>
				
					<div>No discount</div><div>code required</div>
					
				<% end %>
				</div>
			
		</div>
		
		</div>
	</div>
	
	<div class="appinfo-edit">
		<p><a href="/developer/editapp?appid=<%= a.id %>">Edit</a> | <a href="/developer/deleteapp?appid=<%= a.id %>">Delete</a></p>
	</div>
</div>


<% end %>

<p>
<a href="/developer/editapp">Register a new application</a>
</p>